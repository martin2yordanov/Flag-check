{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "upstream=$(git rev-parse --abbrev-ref --symbolic-full-name @{u} 2>/dev/null); if [ -n \"$upstream\" ] && [ \"$(git rev-list --count @{u}..HEAD 2>/dev/null)\" -gt 0 ]; then git push --quiet >/dev/null 2>&1; fi; exit 0",
            "async": true,
            "timeout": 60,
            "statusMessage": "Auto-push to GitHub (Vercel will deploy)"
          }
        ]
      }
    ]
  }
}
